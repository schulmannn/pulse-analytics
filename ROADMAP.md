# pulse-analytics → публичный SaaS: Roadmap по спринтам

> Хэндофф-документ (память чата заканчивается). Здесь контекст + план. Память проекта Claude (MEMORY.md) тоже актуальна.

## Контекст для нового чата

**Что это:** дашборд аналитики Telegram-канала. Repo `github.com/schulmannn/pulse-analytics`, локально `C:\Tools\pulse-analytics`. Деплой на **Railway** (домен `pulse-analytics-production-daf3.up.railway.app`), один Docker-контейнер: Node/Express (`server/index.js`, фронт `public/index.html`) + Python/Telethon MTProto (`mtproto/service.py`, внутр. :8001). **Postgres** подключён (история + баги). `gh` авторизован как schulmannn.

**Что уже сделано (этот цикл):** альбом-фикс (grouped_id); ER/ERV/виральность по постам; хэштег-lift; авто-дайджест «итоги недели» (rule-based); heatmap «когда постить»; velocity «жизнь поста»; мониторинг упоминаний (searchPosts, on-demand, квота ~10/день); архив истории в Postgres (channel_daily/posts/mentions) + ежедневный GH Actions ingestion; баг-трекер со статусами + скриншоты (bytea, хардненинг после адверсариал-ревью).

**Ключевые грабли (НЕ повторять):** `views_graph` из GetMessageStats — ИНКРЕМЕНТАЛЬНЫЙ и ДНЕВНОЙ; альбомы = grouped_id; приватный Railway DATABASE_URL без ssl; глубокая статистика TG только у аккаунта-**админа** канала (бот не даёт); searchPosts квота ~10/день; ingest-токен слать заголовком (URL ломает спецсимволы).

**Текущая авторизация:** один общий `TEAM_PASSWORD` + stateless HMAC. НЕ мультиюзер.

## Архитектурное решение: Variant A (collector на стороне юзера)

Публичный SaaS строим так, чтобы **НЕ хранить сессии юзеров** (в TG нет read-only/скоуп-токена; StringSession = полный доступ к аккаунту → юр-риск + риск банов).

**Поток:** юзер при онбординге: (1) создаёт свои api_id/api_hash на my.telegram.org; (2) запускает наш **collector-агент** у себя (one-click: GitHub Actions template / Docker / десктоп-хелпер — переиспользовать паттерн `notem-mention-monitor`) со своей сессией ЛОКАЛЬНО; (3) агент считает метрики и шлёт нам **только готовый JSON** по per-user API-ключу. Мы храним лишь производные данные. Детализация — полная (агент = админ локально). Опц. платный «managed» тариф: подключение через ОТДЕЛЬНЫЙ TG-аккаунт-админ + шифрование at-rest + явное согласие + лёгкий revoke.

---

## СПРИНТЫ

### Sprint 0 — Быстрые фиксы текущего продукта (делать СРАЗУ, без архитектуры)
| # | Задача | Приоритет | Труд |
|---|---|---|---|
| 1 | 🔴 **Графики размываются на истории >30 дней** — мы это и сломали, добавив архив. Чинить рендер длинных серий (даунсэмплинг точек/адаптивная плотность/ширина шага). | P0 | S–M |
| 2 | В полноэкранной модалке графика — свой выбор timeframe (поверх мастер-фильтра). | P1 | M |
| 3 | В форму бага — категория **Баг / Новая фича / Просьба об изменении** (юзеры предлагают идеи). Расширить существующий баг-трекер (поле `kind`). | P1 | S |
| 13 | Sticky-хедер при скролле (для дашборда — да, удобно). | P2 | S |
| 12 | Футер (ссылки, копирайт). | P2 | S |
| 15 | *Решение:* пай-чарты — использовать минимально (плохо сравнивать >3 категорий; наши breakdown-бары лучше). Только там, где 2–3 доли. | dec | S |

### Sprint 1 — Фундамент SaaS №1: мультитенантность + collector (ГЛАВНЫЙ enabler)
| # | Задача | Приоритет | Труд |
|---|---|---|---|
| — | **Collector-агент (Variant A)** + per-user API-ключ + эндпоинт приёма JSON. Ядро всего паблика. | P0 | L |
| 8 | **Регистрация/авторизация** (email+пароль, верификация, сброс, сессии). `user_id` во ВСЕ таблицы, изоляция данных. | P0 | L |
| — | Переархитектура модели данных под мультитенант (канал/история/упоминания/баги — per user). | P0 | L |
| 10 | *Ответ:* Railway инфраструктуру тянет (домен, Postgres, реплики); бутылочное горлышко — не Railway, а MTProto-доступ (решён collector'ом). | dec | — |

### Sprint 2 — Фундамент SaaS №2: монетизация + онбординг + go-live
| # | Задача | Приоритет | Труд |
|---|---|---|---|
| 9 | **Оплата.** RU-карты + загран → смотреть **Paddle/LemonSqueezy** (merchant-of-record, НДС берут на себя; RU-карты — проверить отдельно, возможно нужен доп. провайдер типа ЮKassa). Планы: Free (лимиты) + Premium ($10/мес, ≤ конкурентов) + 30 дн trial. Гейтинг фич. | P0 | L |
| 20 | Страница **How-to**: подключение TG API (api_id/сессия/запуск агента) + troubleshooting + FAQ. Без неё онбординг Variant A не взлетит. | P0 | M |
| 17 | **Landing/Home**: рега + инфо о нас + ссылки на ресурсы. | P0 | M |
| 11 | Переезд на **.com** (Railway: добавить домен, CNAME, авто-TLS — легко; делать когда лендинг готов). | P1 | S |
| 18 | *Решение ДО лендинга/домена:* ребренд Pulse? имя + лого. | dec | M |

### Sprint 3 — UX-полиш
| # | Задача | Приоритет | Труд |
|---|---|---|---|
| 7 | Тёмная тема (CSS-переменные уже есть — добавить тёмный набор + переключатель, хранить в localStorage/per-user). | P1 | M |
| 6 | Левый тулбар/навигация (редизайн — *уточнить ТЗ*). | P1 | M |
| 16 | Скрытие/ресайз/драг блоков, сохранять **per-user** (зависит от auth из Sprint 1). | P1 | M–L |
| 19 | Мультиязычность eng/rus/spanish по locale (i18n-словари). | P1 | M–L |
| 14 | *Решение:* Instagram-вкладка — починить или убрать (сейчас сломана/игнор). | dec | M |

### Sprint 4 — Совместная работа над багами (зрелость трекера)
| # | Задача | Приоритет | Труд |
|---|---|---|---|
| 5 | Дискуссии по багам + **push/notifications** автору + он сам подтверждает фикс. Флаг **клиент vs наша команда** (клиентам тред-обсуждение не нужно). Slack — опционально как внутр. канал команды. | P1 | M–L |
| 4 | Claude сама вносит изменения по багам (эксперимент: баг → агент → PR). Высокий риск, делать осторожно/полуавтомат. | P2 | L |

### Sprint 5 — Рост и продвинутое (research + nice-to-have)
| # | Задача | Приоритет | Труд |
|---|---|---|---|
| 22 | Интеграция Claude прямо на сайте (свой MCP-коннектор, «AI-friendly», юзер цепляет свой AI). Дифференциатор, но advanced → отложить. | P2 | L |
| N1 | Автоматизации + публичный API + ключи для клиентов. | P2 | L |
| N2 | AI-аналитика через отдельные токены клиентов. | P2 | L |
| N3 | Шедулинг постов из сайта (IG особенно — у них боль с планированием на месяц+). Большая отдельная поверхность. | P2 | L |

---

## Параллельный research-трек (без кода; сделать РАНО, до фиксации цены)
- **#21 Глубокий ресёрч конкурентов**: цены, (если достать) число клиентов, уникальные фишки, чем лучше. → кормит решение по цене (#9). Можно прогнать многоагентным workflow + deep-research.
- **#23 SMM-стратегия**: каналы, примерные бюджеты, окупаемость, самый лёгкий старт.

## Решения, которые блокируют другое (принять заранее)
1. **Ребренд/имя/лого (#18)** → до .com и лендинга.
2. **Цена/планы (#9)** → после ресёрча конкурентов (#21).
3. **IG-вкладка (#14)** — чинить или дропнуть.
4. **Discussions: Slack vs in-app (#5)**.

## Прямые ответы на вопросы из списка
- **#10 Railway не ляжет?** Для старта-среднего — нет; домен/Postgres/реплики ок. Узкое место — MTProto-доступ, решается collector'ом, не хостингом.
- **#11 .com?** Да, тривиально (Railway custom domain).
- **#13 Фриз хедера?** Да, sticky — хорошо для дашборда.
- **#15 Пай-чарты?** Минимально, breakdown-бары лучше.
- **#22 Claude-интеграция?** Классный дифференциатор (MCP), но отложить до стабильного ядра.

## Рекомендуемый порядок
Sprint 0 (быстрые победы, текущий продукт) → research-трек (#21/#23, параллельно) + решения по ребренду/цене → Sprint 1 (мультитенант+collector) → Sprint 2 (оплата+лендинг+.com) → Sprint 3 (UX) → Sprint 4 (коллаб по багам) → Sprint 5 (рост/advanced).
