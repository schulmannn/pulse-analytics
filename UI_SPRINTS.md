# UI Sprints — трекер (loop-режим)

Source of truth прогресса между итерациями. Статусы: TODO / IN-PROGRESS / SHIPPED `<commit>`.
Правила: одна подзадача за итерацию, до прода целиком (дизайн → реализация → гейт → деплой → живая проверка).

## Спринт 1 — Хром и полировка

- [x] **1.1 Меню аккаунта (Claude-style)** — SHIPPED `c87140e`
  AccountMenuContent: identity-шапка (аватар 36px + имя + email + чип «Админ»), группы через hairline-разделители
  (Настройки+Тема / Админ+Баги / Выйти), пункты 14px foreground + muted-иконки + hover-row, шелл rounded-xl
  bg-popover p-1 (как парящий сайдбар), «Тема» = сегмент светлая/системная/тёмная (3 состояния, раньше только toggle),
  иконки monitor+logout. Референс снят с живого claude.ai (272px/12px/п.6×10/h-32).
- [x] **1.2 Настройки — редизайн** — SHIPPED `1de6455`
  Убит вложенный двойной нав (внутренний рельс Профиль/Оформление/Безопасность → секции левого мини-нава диалога,
  scroll-spy удалён); группы де-картонены в open hairline-ledger (без рамок-боксов, divide-y + top-hairline при
  заголовке); строки py-4 без инсета, контрол по центру вертикали (Claude-анатомия); тема = pill-сегмент с иконками
  (светлая/системная/тёмная); кнопки-хром → btn-pill; контент max-w-640; иконки sun/moon/monitor/lock в primitives.
- [x] **1.3 Подписка (UI, без бек-ограничений)** — SHIPPED `5b623b2`
  Секция «Подписка» в Настройках: строка текущего плана + 3 колонки сравнения Free $0 / Pro $12/мес / Max $29/мес
  (фичи с check-иконками, активный план = border-primary + чип «Текущий», CTA «Перейти на …»). Клиентский флаг
  `pulse_plan` в lib/plan.ts (localStorage + useSyncExternalStore, +5 unit-тестов) — БЕЗ бек-ограничений, честная
  нота «оплата не подключена». Юзер-строка сайдбара двухстрочная (email + «План X», Claude-style), пункт «Подписка»
  в меню аккаунта. Гейт для 1.4 = isPaidPlan().
- [x] **1.4 «Добавить участника» (UI при активной подписке)** — SHIPPED `50a8d52`
  Секция «Команда» в Настройках: на Free — upsell с CTA «Смотреть тарифы» (→Подписка); на Pro/Max — ростер
  (владелец + приглашённые: аватар-инициалы, роль Редактор/Наблюдатель select, удалить ×) + форма инвайта
  (email + роль + лимит мест 3/10 по плану, дизейбл при заполнении). Заглушка честная: «письмо не отправляется,
  доступ не выдаётся». Стор lib/team.ts (localStorage `pulse_team`, стабильный snapshot для useSyncExternalStore,
  +6 unit-тестов). Бек не тронут.

## Спринт 2 — Редактор графика (steep-style)

- [x] **2.1 Карусель карточек-превью типов графика** — SHIPPED `5c91c56`
  VariantCarousel в EditWidgetDialog (ChartWidget.tsx): активная карточка центрируется на translated-треке
  (transition 300ms, motion-reduce off), соседи peek по краям (scale .96 + opacity .6), стрелки ‹ › по бокам
  (disabled на краях), точки-пагинация снизу (активная вытянута bg-primary), pointer-свайп ≥40px с подавлением
  click-after-drag; клик по соседней карточке тоже выбирает и центрирует. Live-превью/масштаб wide-вариантов
  и наследование акцента/тинта сохранены. Скролл-лента удалена.
- [x] **2.2 Форма настроек до steep-набора (где применимо)** — SHIPPED `0c1226c`
  Добавлено в диалог «Изменить»: **Грануляция** (день/неделя/месяц; неделя=Monday-anchor, месяц=calendar; суммы —
  flow-метрики) + **«Включая сегодня»** (off = дроп частичного последнего дня) + **«Целевой уровень»** (пунктирная
  goal-линия на line-чартах через WidgetTargetContext — работает в карточке И в overlay). Prefs: grain/includeToday/
  target (+emptiness-guard). Threading: variants-fn получил 2-й арг WidgetSeriesOpts; opt-in флаг `seriesOptions`
  на ChartSection. Покрытие: «Просмотры» + «Репосты» (windowGraphSeries+opts); диалог max-h-85vh.
  **N/A по модели данных (задокументировано):** Metric — виджет и есть метрика (metric-select есть в Report-блоках);
  Breakdown/Filter — нет дименсий у series (breakdowns = отдельные виджеты); Comparison period → Спринт 3.3.
  Follow-up: grain в Tier-2 expand-эксплорер; target на BarChart; rollout seriesOptions на остальные daily-виджеты.

## Спринт 3 — Full-screen графики + визуальное сравнение

- [x] **3.1 Full-screen explorer с реальными осями** — SHIPPED `d6747cc`
  ChartExpandOverlay → настоящий full-screen (Card h-full flex-col в inset-p4, chart-регион flex-1 измеряется
  ResizeObserver → высота в ExpandedChartHeightContext — чарт заполняет весь экран, статы/шапка shrink-0,
  min 240px, фоллбэк 400). LineChart в axes-режиме рисует РЕАЛЬНУЮ ось X внутри svg: stride-тики по ширине
  (~90px/лейбл, всегда первый+последний, анти-коллизия), tick-марки + даты в нижней полосе padB=30;
  3-лейбловый HTML-ряд остаётся только на axis-free карточках. Заголовок+период уже были.
- [x] **3.2 Выбор типа графика + грануляция + тултипы в explorer** — SHIPPED `39a2f02`
  Explorer получил сегмент «День/Неделя/Месяц» (стейт в оверлее, гейт `grainable` в ChartExpandConfig —
  скрыт для level-серий, где суммирование врёт); renderExpanded/Bar/statsFor получили 2-й арг grain;
  подключены «Просмотры»+«Репосты» (тот же windowGraphSeries). Line↔bar тоггл и hover-тултипы уже были
  (v1.1/ChartTooltip). Отдельный «area»-тип НЕ добавлен осознанно: line уже рисуется с градиент-заливкой
  области — дубль-тип без информации. Rollout grainable на остальные flow-виджеты = mechanical follow-up.
- [x] **3.3 Визуальное сравнение: период сравнения НА графике вторым цветом** — SHIPPED `ea2d21d`
  Ghost-серия LineChart из блеклого muted-пунктира → **вторая серия цветом `--chart-2`** (оранжевый
  colorblind-safe контраст-напарник бренд-синего, dasharray 5·4, 1.8px) + **встроенная легенда** под чартом
  (свотч текущего + пунктир-свотч сравнения, `ghostLabel`-проп — MetricPage передаёт своё cmpLabel) +
  **обе серии в hover-тултипе** («… · пред. N»). Внешние хинты «Пунктир — …» удалены (Просмотры по дням,
  MetricPage). Действует везде, где ghost: «Просмотры по дням» (prev-14д), metric-страницы (toggle сравнения).
  **Честное ограничение:** IG PeriodCompareBlock (текст) НЕ заменён — у IG-агрегатов НЕТ дневных серий
  (total_value = 2 синт-точки), рисовать «сравнение на графике» там нечем; текст остаётся правильной формой.

## Спринт 4 — Instagram мульти-аккаунт

- [x] **4.1 Несколько IG-аккаунтов как отдельные источники** — SHIPPED `23cf827`
  **Арх-решение: PK ig_accounts НЕ трогаем** — второй+ IG = отдельный `channels`-row `source='ig'`
  (tg_channel_id NULL): channels уже tenant, изоляция данных (cache-ключи по accountId + query-keys по
  channelId) уже была с b4470c3. Бек: `createIgChannel`/`findIgChannelByIgUser` (db.js), OAuth start
  `?new_source=1` (state.ns, без канала), callback ns-ветка — дедуп по ig_user_id (реконнект той же
  учётки обновляет токен, НЕ дублирует источник) или создание source='ig' + редирект `ig=connected&ch=<id>`.
  Фронт: useConnectIg({newSource}) (void-трюк для mutate()), IgFeed-баннер переключает на новый источник
  (setChannelId+invalidate channels), свитчер: IG-only каналы скрыты из Telegram-группы, кнопка
  «Подключить ещё один аккаунт» в /connect (connected-ветка, с нотой про смену профиля), SourceStatus
  'ig'-ветка в настройках Каналов. Гейт: node --check + 16 server + build + 145 front.
  **Живой e2e второго аккаунта = юзер** (нужен Allow под другой IG-учёткой; в dev-режиме Meta аккаунт
  должен быть Instagram-тестером app). Delete IG-источника = обычное удаление канала (FK каскад чистит токен).

## Спринт 5 — Аудит дашборда

- [x] **5.1 TG-аудит дефолтных раскладок** — SHIPPED `a6d72c0`
  **Дубль views-пары РЕШЁН:** «Просмотры по дням» (views_summary, фикс-14д+ghost) теперь честный fallback
  ТОЛЬКО для каналов без graphs-серии (`!viewSeries` — мелкие/QR, где broadcast-статистики нет); на больших
  каналах остаётся один rich «Просмотры» (expand/grain/target), сравнение периодов — на /metrics/views
  (ghost-тоггл). Прочее по свипу: дубль топ-постов на /posts уже был убран ранее (комментарий в Posts.tsx:48),
  4 growth-виджета Динамики уже дедупнуты в v5 (3a3af44), порядок табов уже выровнен с IG. Обзор (hero/Инсайт/
  Рост/Топ) курирован, Посты=топ-25 чисто, Упоминания без дублей. Итог: лента без повторов, порядок
  динамика→контент→аудитория выдержан.
- [x] **5.2 IG-аудит** — SHIPPED `dad4c63`
  Живой проход всей IG-ленты (@bynotem, все 4 блока deep-link'ами): заголовки уникальны, порядок
  движение→охваты→сравнение→инсайты→контент→аудитория логичен. **Один найденный косяк починен:**
  «Аудитория» дублировалась подряд (заголовок фид-блока + первая секция панели) → секция переименована
  в «Демография» (точнее: Возраст/Пол/Топ стран/Топ городов). Пары «Топ публикаций» (Обзор, top-3) vs
  «Лучшие публикации» (Контент, полный грид) и «Движение аудитории» (Обзор-выжимка) vs «Движение
  подписчиков» (Аналитика, с пред-периодом) — НЕ дубли, а курируемый обзор+детальный разбор (тот же
  паттерн, что у TG). Пустых/шумных секций нет (best-time/stories и т.п. уже гейтятся на hasSignal).

## Спринт 6 — Анимации

- [x] **6.1 Сдержанные анимации ключевых элементов** — SHIPPED `PENDING_HASH`
  **Entrance:** тихий fade-rise (6px, 350ms, cubic-bezier(.2,.7,.3,1)) каждой виджет-карточки при маунте
  (открытие ленты/переключение таба), stagger 35ms×slot (кэп 8) через `--enter-delay` от seqIndex;
  на ВНУТРЕННЕМ слое карточки (внешний section несёт FLIP-transform — не конфликтуют), в reorder-режиме
  класс уступает jiggle (оба владеют animation). `prefers-reduced-motion: reduce` → animation none.
  **Hover:** hairline карточки чуть углубляется (`hover:border-ink3/40`, transition-colors) — жизнь без теней.
  **Каунтеры чисел НЕ добавлены осознанно** (в задаче со знаком вопроса): бегущие цифры на паре с
  tabular-nums-леджерами читались бы как AI-блинг — против «отредактированного» вида. Переходы
  меню/кнопок/карусели/оверлеев уже были (transition-colors/300ms track/FLIP).
