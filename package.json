{
  "name": "pulse-analytics",
  "version": "1.0.0",
  "description": "Social media analytics backend for Instagram & Telegram",
  "main": "server/index.js",
  "scripts": {
    "migrate": "node server/migrate.js",
    "start": "node server/migrate.js && node server/index.js",
    "worker": "node server/migrate.js && node server/worker.js",
    "dev": "node server/migrate.js && node --watch server/index.js",
    "test": "node scripts/run-tests.mjs",
    "lint": "biome lint --diagnostic-level=error",
    "check:syntax": "node scripts/check-server-syntax.mjs",
    "check:migrations": "node scripts/check-migrations.mjs",
    "check:boundaries": "node scripts/check-server-boundaries.mjs",
    "check:lintdebt": "node scripts/lint-debt.mjs",
    "check": "npm run check:syntax && npm run check:migrations && npm run check:boundaries && npm run check:pypins && npm run check:lintdebt && npm run lint && npm test",
    "check:pypins": "node scripts/check-python-pins.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0",
    "compression": "^1.8.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "express-rate-limit": "^8.7.0",
    "pg": "^8.23.0",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.11"
  },
  "engines": {
    "node": ">=20.19"
  }
}
