-- Абсолютный уровень подписчиков IG на момент дневного сбора (профильный followers_count).
-- ig_daily.followers хранит GROSS дневной приход (канон PR #100: follower_count = приход БЕЗ
-- вычета отписок), уровня в архиве не было — график «реального движения базы» (аналог ТГ
-- «Подписчики») приходилось бы выводить только реконструкцией от живого значения. Крон теперь
-- ежедневно фиксирует реальный уровень; фронт использует эти точки как якоря и реконструирует
-- дни до/между ними по чистому движению follows − unfollows.
ALTER TABLE ig_daily ADD COLUMN IF NOT EXISTS followers_total INTEGER;
