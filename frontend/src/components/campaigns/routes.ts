/**
 * Канонический путь списка кампаний. Кампании — не отдельный маршрут, а второе представление
 * раздела «Контент» (`?view=campaigns`, см. panels/Posts.tsx), поэтому строка нужна и странице
 * кампании («К списку кампаний»), и ⌘K-палитре. Модуль намеренно без React-импортов: его тянет
 * lib/paletteCommands (чистое построение команд).
 */
export const CAMPAIGNS_LIST = '/posts?view=campaigns';
