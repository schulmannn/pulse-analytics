/**
 * Тексты ⓘ метрик МойСклада из каталога виджетов. Величины склада (рубли/заказы) — СВОИ и никогда
 * не смешиваются с просмотрами/охватом соцсетей. Синхронно их читает lib/widgetMetrics.ts, лениво —
 * msMetricDetails через loadMetricDetails('ms').
 */
import type { MetricInfo } from '@/lib/metricDetails';

export const MS_METRIC_INFO: Readonly<Record<string, MetricInfo>> = {
  'ms.revenue': {
    label: 'Выручка',
    formula: 'Сумма продаж по дням за окно.',
    included: 'Возвраты считаются отдельно и из выручки не вычитаются.',
    sourceNote: 'МойСклад (серверный отчёт; «Всё» — дневной архив).',
  },
  'ms.orders': {
    label: 'Заказы',
    formula: 'Число заказов покупателей по дням за окно.',
    sourceNote: 'МойСклад.',
  },
  'ms.avgCheck': {
    label: 'Средний чек',
    formula:
      'Сумма заказов дня ÷ число заказов дня; хедлайн — сумма заказов окна ÷ число заказов окна (не среднее дневных чеков).',
    included: 'Дни без заказов в серию не входят (нет чека — нечего усреднять).',
    sourceNote: 'МойСклад.',
  },
};
